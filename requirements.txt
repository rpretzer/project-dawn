# Project Dawn - Python Dependencies
# ===================================
# Core dependencies (required)
# ----------------------------
aiohttp>=3.9.0
aiofiles>=23.2.0
asyncio-extras>=1.3.2

# LLM Integration (required)
# -------------------------
tiktoken>=0.5.0
backoff>=2.2.0

# Data Processing (required)
# --------------------------
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0
h5py>=3.9.0
msgpack>=1.0.7

# Database & Storage (required)
# -----------------------------
chromadb>=0.4.0
pydantic-settings>=2.0.0  # Required for chromadb compatibility
# sqlite3 is built-in to Python, no installation needed

# Graph & Network Analysis (required)
# -----------------------------------
networkx>=3.1

# Web & API (required)
# --------------------
websockets>=12.0
PyJWT>=2.8.0

# Utilities (required)
# --------------------
python-dotenv>=1.0.0
pyyaml>=6.0.1
bcrypt>=4.0.0  # Secure password hashing
watchdog>=4.0.0  # Plugin hot-reload support

# Optional Dependencies
# =====================
# These are optional and the system will work without them (with reduced functionality)

# Blockchain & Web3 (optional - for blockchain features)
# ------------------------------------------------------
# web3>=6.11.0
# eth-account>=0.9.0

# P2P Networking (optional - may have compatibility issues)
# ---------------------------------------------------------
# libp2p>=0.1.0
# multiaddr>=0.0.9
# Note: libp2p Python library may not be fully compatible with Python 3.14
# The system will use fallback networking if libp2p is not available

# IPFS (optional - requires IPFS daemon)
# ---------------------------------------
# ipfshttpclient>=0.8.0a2
# Note: Requires IPFS daemon running. Install separately: https://ipfs.io/

# Computer Vision (optional)
# --------------------------
# opencv-python>=4.8.0

# Social Media Integration (optional)
# -----------------------------------
# discord.py>=2.3.0
# tweepy>=4.14.0
# linkedin-api>=1.0.0

# Observability (optional)
# ------------------------
# opentelemetry-api>=1.21.0
# opentelemetry-sdk>=1.21.0

# Development / Testing (recommended for contributors)
# ----------------------------------------------------
pytest>=9.0.0

