# Project Dawn V2 - Requirements

# WebSocket support
websockets>=12.0
cryptography>=41.0
zeroconf>=0.131.0  # Optional: for mDNS discovery

# Libp2p support (Phase 3)
# Note: py-libp2p is available but may have limited features
# Alternative options: libp2p-js bridge or libp2p-rs FFI
libp2p>=0.1.0  # Python Libp2p implementation

# JSON-RPC 2.0 (built-in, no external dependency needed)

# Testing (optional)
pytest>=7.0
pytest-asyncio>=0.21.0

